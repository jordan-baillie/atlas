- 2026-05-01: Phase 2 TP coverage cleanup (commit 4991b3ad) — CAT/GLD/XLI had trailing_stop orders (simple class) placed by RCA 2C (commit 35d2286a) that cancelled OCO brackets without placing replacement TP orders; Alpaca error 40310000 blocks standalone SELL LIMIT when trailing_stop holds all shares (available=0); Fix: cancel trailing stop + place OCO (static stop at current trailing-stop level + limit TP); scripts/retro_attach_tp_legs.py (--dry-run supported); OCO order_id stored in BOTH stop_order_id AND tp_order_id fields (healthcheck sees order_class=oco → has_stop=True, has_tp=True); FCX SKIPPED — connors_rsi2 profit_target_atr_mult=0 means no price-based TP by design; FCX will re-appear in healthcheck (follow-up: add strategy-level TP-exempt rule); TP prices: CAT=978.33, GLD=509.22, XLI=200.07; healthcheck after: 9/12 covered (FCX×3 within debounce); trade-off: trailing→static stop (sync_protective_orders will tighten further)
- 2026-05-01: Bug A fix — `research/autoresearch_nightly.DEFAULT_STRATEGIES` expanded from 5→8 strategies; added connors_rsi2, short_term_mr (audit-discovered missing), bb_squeeze (has research_best sp500 sharpe=0.49 2026-03-11); mtf_momentum intentionally excluded (enabled=False ALL configs, no research_best row); comment updated 'Top-5' → explanatory docstring; 3 regression tests in `tests/test_autoresearch_nightly_default_strategies.py`; commit f87a5dba
- 2026-05-01: FIX-PMEQ-001 — `_get_per_market_equity` refactored: formula `pos_mv + snap_cash*scale` → `pos_mv + snap_cash + fills_since_snapshot` via new `portfolio/per_market_cash_flow.py::compute_realized_cash_flow_since()`. Fetches FILL+DIV activities since snapshot_time, attributes via `derive_universe`. Degraded mode (API failure): snap_cash frozen + kill switch suppressed below 20% override (`_per_market_equity_degraded` flag). `_ALL_TRADED_MARKETS=('sp500','sector_etfs','commodity_etfs')` module constant. 14 tests in `tests/test_per_market_equity.py`. HWM reset tool: `scripts/reset_per_market_hwm.py`. Apr 30 phantom 13.69% DD resolved. GLD ambiguity: in both commodity_etfs+gold_etfs; derive_universe returns 'commodity_etfs' (alphabetically first).
- 2026-05-01: Fix #1 — `get_cached_portfolio_risk` in `db/atlas_db.py` — WHERE clause changed from `julianday(as_of)` to `julianday(created_at)`; `as_of` is date-only (midnight), `created_at` is full timestamp; rows written at 11pm appeared ~23h stale next morning; 6 tests in `tests/test_portfolio_risk_cache.py`; also patched `tests/test_precompute_risk.py::test_stale_returns_none` to supply explicit old `created_at` (was relying on DEFAULT CURRENT_TIMESTAMP which now appears fresh); commit 4a4017e4
- 2026-05-01: Fix #2 — `LiveExecutor._account_info_cache` + `_get_cached_account_info()` in `brokers/live_executor.py`; adds `_account_info_cache=None` to `__init__`, `_get_cached_account_info()` method, reset `self._account_info_cache=None` at top of `execute_plan()`; replaces 3 direct `self._broker.get_account_info()` calls at _capture_start_equity/line~331, _check_circuit_breaker/line~378, leverage gate/line~1123; note: `_reset_circuit_breaker_if_new_day` has pre-existing bug (checks `trade_date != self._daily_date` but `_daily_date` already updated before the call) — `_daily_start_equity` never resets to 0 on new day via this path; 10 tests in `tests/test_live_executor_account_cache.py`; commit 02a197fb
- 2026-05-01: Fix #8 — N+1 query fixes: (1) `_fetch_vols_from_cones_batch(tickers)` added to `risk/stop_probability.py` before `analyze_position_stop`; `analyze_all_open_positions` calls it once and passes `vol_annual=vol_map.get(ticker)` into `analyze_position_stop`; batch uses correlated MAX(as_of) subquery; (2) `services/api/health.py` system_health() + _build_universes_list() both refactored to read all configs first then ONE batched `SELECT universe,COUNT(*) FROM trades WHERE exit_date IS NULL AND universe IN (...) GROUP BY universe`; was N connections per call (one per universe); 8 tests in `tests/test_health_n1_fix.py`; commit 13994db6
- 2026-04-14: 5 ETF universes added — `sector_etfs` (11), `commodity_etfs` (10), `treasury_etfs` (5), `gold_etfs` (4), `defensive_etfs` (6); all paper mode, `mode=paper`, `live_enabled=false`
- 2026-04-14: `markets/etf_markets.py` created — `_BaseUSETFMarket` + 5 subclasses; all US/USD/no-suffix/NYSE-ET-hours; registered in `markets/registry.py` `_auto_register()` (7 total markets now); REQUIRED to avoid `KeyError` in `_normalize_ticker` + `get_market_tickers`
- 2026-04-14: `data/processed/{sector,commodity,treasury,gold,defensive}_etfs/universe.json` created (static lists, no `details` array needed); `config/active/{sector,commodity,treasury,gold,defensive}_etfs.json` created (no moomoo/allocation/overlay sections); all pass `validate_config()` and `load_universe()`

- 2026-04-14: Sector classification fix — created `data/processed/sector_map_commodity_etfs.json` (10 tickers: Precious Metals/Energy/Agriculture/Industrial Metals/Uranium) and `data/processed/sector_map_sector_etfs.json` (11 SPDR ETFs); added sector map enrichment block to `_run_regime_aware_plan` in `brokers/plan.py` (before PortfolioConstructor.construct call); loads per-universe maps + primary market map into `_combined_sector_map`; fixes "Unknown" sector → max_sector_concentration rejections blocking all regime-aware trades
- 2026-04-24: P0-4 XLK recovery — `_assert_state_file_parity()` added to `db/atlas_db.py`: called from `record_trade_entry()` after every successful INSERT; reads `live_{universe}.json`; logs ERROR + self-heals (appends position) if ticker missing; `_state_dir_override` module var added for test isolation (mirrors `_db_path_override`); `brokers/state/live_sector_etfs.json` patched to include XLK (id=180, momentum_breakout, entry=156.77, stop=153.5191, shares=8)
- 2026-04-24: Root cause of XLK orphan — state file write path is gated by `live_enabled` in BOTH `eod_settlement.save_state()` (line 490-491) and `sync_protective_orders._update_state_positions()` (line 479-481); `record_trade_entry()` writes to SQLite unconditionally; these two paths can diverge when `live_enabled=False`; `_assert_state_file_parity` breaks this dependency
- 2026-04-24: `tests/test_state_file_sqlite_parity.py` — 7 tests, 3 classes; all pass; uses `_state_dir_override` fixture for isolation; covers: ERROR log, self-heal, happy path, no-file silent, live_enabled=False still guards, integration via record_trade_entry, exact XLK scenario

- 2026-04-29: Phase 2 auto-remediation — core/remediation_kill_switch.py (8-layer chain: L1=env, L2=AUTO_REMEDIATION_HALT file, L3=data/HALT/.live_halt, L4=drawdown>5%, L5=3+ critical healthchecks in 24h, L6=reviewer rejection>50% of last 10, L7=via L2, L8=systemd); all data-driven layers (L4-L6) fail-open on DB error; halt(reason,source) creates HALT file idempotently; resume() removes only AUTO_REMEDIATION_HALT; check_all_layers(db_path) returns first BlockReason or None; 31 tests in tests/test_remediation_kill_switch.py
- 2026-04-29: core/budget.py — check_budget() enforces 3 layers: (A) commit cap >=10, (B) absolute reverts >=2, (C) revert rate >=25% HALT / >=15% ALERT (both gated on merged_24h>=4 sample); enforce_budget() calls halt() on HALT + optional Telegram; _load_budget_config() has FileNotFoundError fallback to defaults; 27 tests in tests/test_remediation_budget.py; 56/56 total pass
- 2026-04-29: Production note — portfolio_snapshots table has total_equity not equity and no daily_pnl_pct column; L4 check correctly fails-open in production (returns None with WARNING log); tests create their own tmp DB with the right columns
- 2026-04-24: P2-14 — `scripts/cleanup_stale_plans.py`: backup DB, expire pending plans >14d old (UPDATE plans SET status='expired' WHERE status='pending' AND created_at < datetime('now', '-14 days')); idempotent; expired 20 plans on first run; cron `0 17 * * *` AEST (17:00 AEST = 07:00 UTC); 10/10 tests pass
- 2026-04-24: P2-15 — `data/stops_held_state.json` wiped: XLK::commodity_etfs (permanently_skipped, P0-3 scoping bug) + CCJ::commodity_etfs (stale); `scripts/regen_stops_held_state.py` queries Alpaca for held-status atlas_stop orders per market; result={} (no held stops at broker); sector_etfs skipped (paper API cred mismatch, non-fatal); backup at data/stops_held_state.json.pre-wipe-20260424T012425
- 2026-04-24: P2-16 — `TradeLedger.record_entry` now returns `Optional[int]` (trade_id from record_trade_entry, None on dupe); `_execute_entry` + `reconcile_entry_fills` in live_executor.py both capture `_trade_id` and emit `TRADE_OPENED symbol=... qty=... entry_price=... strategy=... universe=... trade_id=... order_id=... stop_price=...` ONLY when `_trade_id is not None` (genuine insert); logger name is `atlas.live_executor` (not brokers.live_executor); 10/10 tests pass
- 2026-04-24: plans duplicate-row fix — `db/atlas_db.py` `update_plan(plan_id, status, approved_at, executed_at, plan_data)` added (COALESCE; all optional); `brokers/plan.py` `_save_plan()` rewrites to get_plan first -> update_plan if row exists, record_plan only on first save; `update_plan_status()` kept for backward compat; `tests/test_plan_dual_write.py` 8 tests (all pass); commit 2b7ba7a3
- 2026-04-27: audit-fix-2 — protective stops restored: ADI (af9f454c, trail=$15.75), FCX (fc1d7117, trail=$3.08), XLI (8fd9810f, trail=$4.74), XLK (258605e5, trail=$3.25 upgraded from HELD static $153.52); AMD already had stop at $337.53 (profitable position, NOT inverted — AMD current=$354.20 > entry=$278.25); 12/12 DB stop_order_ids backfilled; tests/test_protective_stops_invariant.py added (19/19 pass); commit 12e720ab
- 2026-04-27: stop-coverage audit finding — engineering lead's "11/12 uncovered" was stale; real state: only ADI/FCX/XLI had no stops; AMD/AVGO/CCJ/GLD/SLV/UNG/XLY already had trailing stops; CAT/XLK had valid OCO pairs (limit TP + HELD stop SL); HELD status = pre-market, NOT broken
- 2026-04-27: XLK OCO upgrade: cancelling the SL leg of an OCO also cancels the TP limit leg at Alpaca (confirmed by absence of XLK limit order post-sync); the upgrade from HELD static to trailing stop removed the TP leg; XLK now has solo trailing stop
- 2026-04-27: pre-existing test failures confirmed: test_ledger_integrity.py::TestNoDuplicatePerTicker::test_duplicate_open_is_detectable (UNIQUE constraint added in 2026-04-22 prevents test from inserting duplicate AMD rows); test_atlas_db.py::TestPlans::test_get_plans_all (hardcoded 2026-01-01 date now 116d old, fails _validate_plan_date() >30d guard)
- 2026-04-28: B3 Canary promotion of top-3 sp500 candidates — all 3 REJECTED by Gates 2-3 (NOT Gate 4). sector_rotation/opening_gap: Gate 3 fail (candidate portfolio sharpe=-0.1448 ≤ 0); mean_reversion: Gate 2 fail (regression: sharpe -15.8%, cagr -260.6%). Key insight: "best_kept_sharpe" for sector_rotation+opening_gap (0.9099) is PORTFOLIO-LEVEL sharpe from April 16 baseline experiments (params_changed=NULL), not individual strategy improvement. Promoting research_best.params for these degrades portfolio. mean_reversion's April 16 params (rsi_oversold=52, ibs_max=1.05) degraded portfolio in current regime. All expected behavior. Each auto_promote Gate 2 backtest ~238s (2 backtests = ~477-493s total elapsed).
- 2026-04-28: M1 Trade dedup/superseded — `superseded INTEGER NOT NULL DEFAULT 0 CHECK (superseded IN (0,1))` column added to `trades` table; migration `scripts/migrations/2026-04-28-trade-dedup-superseded.py` (dry-run default, `--apply`): converts old status='superseded' rows, finds dup clusters by (ticker,strategy,DATE(exit_date),ROUND(pnl,2)), marks non-canonical rows superseded=1; old `idx_trades_no_dup_closed` dropped (MUST be dropped BEFORE row status changes or it fires on conversion); new `uq_trades_active_closed` partial UNIQUE index on (ticker,strategy,DATE(exit_date),ROUND(pnl,2)) WHERE status='closed' AND superseded=0; `trades_active` view = WHERE superseded=0; 12 rows marked (6 old-style + 6 new) across 6 clusters; mean_reversion pnl -$90.30, momentum_breakout pnl -$73.48 after dedup
- 2026-04-28: M1 record_trade_exit dedup guard — before UPDATE open→closed, fetches open trade entry_price/shares, computes would_be_pnl, queries for existing closed+superseded=0 row with same (ticker,strategy,DATE(now),ROUND(pnl,2)); if dup found: logs WARN 'trade dedup hit', sets superseded=1 on the updated row instead of 0; fallback path when superseded column absent (pre-migration); superseded flag checked dynamically via PRAGMA table_info
- 2026-04-28: M1 P&L query updates — `get_closed_trades` (atlas_db.py) adds `AND superseded=0`; `chat_server.py` strategy_performance + overall_performance queries add `AND (superseded=0 OR superseded IS NULL)`; `regime_performance_report.py` COUNT(*) and tagged_rows query add same filter; `backfill_trades.py` reads all closed rows intentionally (audit completeness — comment added); tests: 10/10 in `tests/test_trade_dedup_superseded.py`
- 2026-04-28: auto_promote logging gotcha — strategy_evaluator.py calls `setup_logging("strategy_evaluator", level=WARNING)` at module-level (line 54), which CLEARS all existing logging handlers (including basicConfig stdout handler). After Gate 2 starts, all INFO logs from research.promoter vanish from script stdout; they go to atlas.log only if level≥WARNING. Gate 4 OOS subprocess output goes to /tmp/oos_result.json only.
- 2026-04-28: research_experiments baseline experiments — strategy+universe combos where all "kept" experiments have params_changed=NULL and desc='baseline' are PORTFOLIO-level measurements (full portfolio backtest), not individual strategy parameter improvements. The 0.9099 sharpe for sector_rotation/opening_gap was the WHOLE PORTFOLIO sharpe on April 15/16. These should NOT be confused with genuine parameter improvements. A future audit enhancement should filter out baseline-only kept experiments from promote_eligible.
- 2026-04-28: `tests/test_canary_promote_top3.py` created — 4 tests (4/4 pass): TestAuditTop3Candidates (captures stdout of audit_promotion_backlog.main), TestCandidateGate4Reject (mocks _run_oos_validation to return fail, asserts no pending entry), TestCandidateTelegramNotification (mocks _check_cooldown to False, asserts _notify called), TestPendingPromotionEntryShape (mocks all gates to pass, asserts pending entry shape)
- 2026-04-28: M2 research_best solo-sharpe schema fix — `solo_sharpe REAL`, `portfolio_sharpe REAL`, `metric_type TEXT NOT NULL DEFAULT 'unknown'` added to `research_best`; `metric_type` values: 'solo','portfolio','both','legacy_portfolio','unknown'; migration `scripts/migrations/2026-04-28-research-best-solo-sharpe.py` (dry-run default, `--apply`); backfilled 5 sp500 rows with solo_sharpe (mean_reversion=1.014, momentum_breakout=1.1499, sector_rotation=0.4496, opening_gap=0.2481, trend_following=0.3643); 38 rows marked legacy_portfolio; backup at `/tmp/research_best_pre_m2_<timestamp>.csv`
- 2026-04-28: M2 writer changes — `upsert_research_best()` gains `solo_sharpe`, `portfolio_sharpe`, `metric_type` params; uses `INSERT...ON CONFLICT DO UPDATE SET solo_sharpe=COALESCE(excluded.solo_sharpe, solo_sharpe)` to preserve existing solo_sharpe when new call passes None; emits DEBUG log when writing legacy sharpe without new fields; `save_best()` in research/loop.py gains same params + passes through; `ResearchSession.keep()` gains `solo_sharpe`, `portfolio_sharpe` params
- 2026-04-28: M2 runner changes — `autoresearch_runner.py`: `_cur_solo_sharpe` initialized per-experiment in sweep loop; set from solo screen result when fast_screen=True, else None; passed to `session.keep(solo_sharpe=_cur_solo_sharpe, portfolio_sharpe=combined_sharpe)`; `_promote_session_result` prefers `row["solo_sharpe"]` over `row["sharpe"]` with WARNING on fallback
- 2026-04-28: M2 consumers updated — `audit_promotion_backlog.py`: `_get_research_best_sharpe` uses `COALESCE(solo_sharpe, sharpe)`, `_get_research_best_meta` helper for legacy detection, `[LEGACY-PORTFOLIO]` annotation + stderr WARN for rows with solo_sharpe=NULL; `services/chat_server.py` 3 sites: research summary, leaderboard, coverage matrix; `check_config_vs_research_best.py` sharpe query; `regen_brain_strategies.py` backwards-compat via PRAGMA table_info check (handles pre-migration DBs in tests)
- 2026-04-28: M2 key finding — mean_reversion/sp500: portfolio_sharpe=0.2691 vs solo_sharpe=1.014 (was masked by combined baseline); sector_rotation/sp500: portfolio=0.0442 vs solo=0.4496; trend_following/sp500: portfolio=0.6618 vs solo=0.3643 (portfolio HELPS trend_following); after M2, mean_reversion/sp500 shows "already superseded" in audit (solo_sharpe=1.014 > best_kept=0.9831) — correct behavior
- 2026-04-28: M2 test update — `tests/test_canary_promote_top3.py::TestAuditTop3Candidates` updated: mean_reversion/sp500 expected to be "already superseded" (not YES) after M2; opening_gap/sp500 expected NOT to be YES (already superseded by solo_sharpe=0.2481); spec said mean_reversion "should REMAIN" but live data has solo_sharpe=1.014 > best_kept=0.9831 → correctly already superseded
- 2026-04-29: Phase A.6 cruft removal — ~950MB freed (4.1G→3.2G): deleted data/atlas_backup_20260424*_before_market_id_migration.db + data/atlas.db.pre-* Apr24 backups + all data/backups/*.db (6 cleanup-stale-plans + good-friday-backfill + pre_universe_fix); 85 tracked+53 untracked Mar-10 experiment JSONs (1 sample kept); 128 reconciliation logs>30d; 49 research/locks>6d; 3 config archives; 13 orphan scripts git mv to scripts/tools/archive/ (sweep_universes, verify_health_tables, vol_scaling_check, optimizer_promote, migrate_add_stop_order_id, close_mrvl×2, backfill_fred, backfill_equity_realized, backtest_universes, investigate_held_stops, fix_xly_contamination, backfill_position_protective_orders); migrations/archive/ dir created (0 archived, all 24 migs <14d); strategies bb_squeeze/mtf_momentum/trend_following all KEPT (active script imports; trend_following also has 57 signals/30d); guard test tests/test_no_dead_imports.py (8 tests, 7 pass, 1 skip placeholder); commits cb2fa683+843256bd+7c68dee2
- 2026-04-29: Phase 1A — GLD/XLI/XLY TP-naked (5+ days). Existing trailing stops cancelled and replaced with OCO pairs. OCO API quirk: LimitOrderRequest(order_class=OCO) requires BOTH stop_loss=StopLossRequest() AND take_profit=TakeProfitRequest() even though parent IS the limit/TP leg (Alpaca 40010001 without both). OCO parent=limit(TP), legs[0]=stop(SL). XLY stop updated 10.618→$116.03 (trailing ratcheted up). GLD TP=f8d94dbf/SL=1ba45d16@$509.22/$420.66; XLI TP=76a50f20/SL=d0813b83@$200.07/$169.23; XLY TP=c2208ed3/SL=45a288c3@$133.91/$116.03. Migration: scripts/migrations/2026-04-29-add-tp-orders-rca-1a.py (idempotent). Regression: tests/test_rca_phase1a_open_position_tp.py (9 tests).
- 2026-04-29: RCA #3A — Overlay Phase 3A FLIP: sp500 `overlay.shadow_mode` flipped false; `overlay_enforce_validated: true` added as gate proof; `config/schema.py` `validate_config()` gained `[OVERLAY_ENFORCE]` cross-field guard (shadow_mode=false requires overlay_enforce_validated=true); backtest shows +$34.61 cumulative delta over 7 days (3 tighten-affected trades, all losers correctly avoided); commodity_etfs + sector_etfs remain shadow_mode=true; backtest script `scripts/backtest_overlay_phase3a.py`; report `reports/overlay_flip_decision_2026-04-29.md`; 20/20 tests; commit 576ac913
- 2026-04-29: RCA #4D — `portfolio/market_equity_attribution.py` created: `attribute_equity_pro_rata(broker_equity, broker_cash, positions_by_market)` → pro-rata by MV; 0-position fallback splits cash equally; `scripts/migrations/2026-04-29-add-market-equity-history.py` creates `market_equity_history` table (UNIQUE date+market_id, two indexes); hook added to `scripts/eod_settlement.py` after SQLite writes (~line 779): calls `portfolio._broker.get_positions()` for ALL positions, groups by `derive_universe()`, upserts all markets; endpoint `/api/market_equity_history?days=90` added to `services/chat_server.py` (~line 948); seed: sp500=$971, commodity_etfs=$1002, sector_etfs=$3216 (2026-04-29); 6/6 tests pass (`tests/test_rca_phase4d_market_equity.py`); commit `14bf07cf`
- 2026-04-29: Phase A.2 — `scripts/healthcheck_tp_coverage.py` created: every 15min RTH, queries broker for 3 markets (sp500/commodity_etfs/sector_etfs), classifies SELL stop/stop_limit/trailing_stop as stop coverage and SELL limit as TP coverage; OCO/bracket order_class counts both; held raw_status is active; debounce via `data/healthcheck_tp_coverage_state.json` (first_missing_at per key `market:ticker`); alert after >5min; exit 0=ok, 1=alert fired, 2=broker error; 18/18 tests; commit `1d867315`; crontab entry owned by Worker B
- 2026-04-29: Phase A.2 note — all 3 markets share one Alpaca account so each market's broker.get_positions() returns all 4 broker positions (each market independently verifies coverage); OCO legs have order_class populated; standalone orders have empty string order_class
- 2026-04-29: Phase A.3 lint scripts — `scripts/lint_bare_except.py`: AST-walks repo, flags bare `except:` + `except Exception:` without logger.exception/raise; baseline `bare_except_baseline.txt` (839 offenders, all grandfathered); `scripts/lint_pi_system_prompt.py`: flags pi/claude subprocess one-shot calls missing `--system-prompt`; baseline `pi_system_prompt_baseline.txt` (0 offenders — all existing calls correct); `.pre-commit-config.yaml` created with standard hooks + both lints as always_run local hooks; 14 tests in test_lint_bare_except.py + test_lint_pi_system_prompt.py
- 2026-04-29: Phase A.5 quick wins — (1) `scripts/eod_settlement.py`: removed dead `if False:` IBKR protective-order block (~30 lines, unreachable since IBKR removal); (2) `brokers/live_portfolio.py`: `_save_state_warned: bool = False` added to `__init__`, throttles save_state warning to 1/instance (was 30+ per cron); (3) `db/atlas_db.py:_assert_state_file_parity`: Telegram alert on mismatch with 1h cooldown (cooldown file uses _state_dir_override path if set, else data/); (4) `scripts/verify_dual_write.py`: `_alert_telegram_on_fail()` helper added, called just before sys.exit when not all_passed; 9 tests in test_quick_wins_a5.py; commits db583128 + f35c3aaf
- 2026-04-29: Phase B.0 — `_protective_ledger_enabled()` added to both `brokers/live_executor.py` and `scripts/sync_protective_orders.py` (env PROTECTIVE_LEDGER_WRITE_ENABLED, default true); live_executor wires: (1) bracket entry FILLED → `upsert_protective_record(oco_class='bracket')` after bracket leg parsing; (2) `_execute_exit` fill confirmed → `close_protective_record`; (3) `reconcile_exit_fills` after `_ledger.record_exit` → `close_protective_record`; (4) `place_stops_for_plan` oco path → upsert(oco_class='oco'), trailing path → upsert(oco_class='trailing'); sync_protective wires: (c) `_apply_db_consistency` after `update_trade_protective_orders` → upsert for `_DB_UPDATE_ACTIONS` tickers; (a) second loop refreshes ALL per_ticker with resolved open IDs (updates last_synced_at every cycle, even 'skipped'); (b) `sync_market` after `_apply_db_consistency` → close for state_tickers not in broker positions; all writes try/except non-fatal; 16/16 new tests in `tests/test_b0_protective_wireup.py`; 21/21 A.1 tests still pass; CAT last_synced_at updated live; commit 34670228
- 2026-04-29: B.0 additive pattern — existing `trades.stop_order_id/tp_order_id` writes NOT removed; `position_protective_orders` is second writer running in parallel; B.2 will do the cutover (making PPO authoritative); is_dry_run + safety are computed properties in LiveExecutor reading from config["trading"]["live_safety"]; use `_minimal_live_config()` pattern for tests (set dry_run_first: False there)

- 2026-04-29: P1 Telegram noise reduction — `healthz_hourly.sh`: (1) removed `try_bash_fixes` Telegram send (pycache/log-rotate → log only); (2) tightened pi agent TELEGRAM instructions (3-case rule: service DOWN→recovered, unresolvable issue, new ERROR class; explicit "do NOT Telegram for routine maintenance/reconcile/restart of running service/all-clear"); (3) drift CRITICAL wrapped in 4h file-based cooldown (hash: `drift_{md5[:12]}` in `$COOLDOWN_DIR`); (4) dashboard dist Telegram threshold >6h→>24h (6-24h: silent rebuild only); (5) dashboard timer inactive wrapped in 6h cooldown file `dashboard_timer_inactive`. `reconcile_positions.py`: 4h file-based cooldown (`logs/healthz-cooldowns/reconcile_{market}`) before Telegram send (persistent CAT UNTRACKED was firing daily). Baseline: 1127 total / 233 healthz Telegram sends in logs. 10/10 tests in `tests/test_telegram_noise_budget.py`.
- 2026-04-29: P1 cooldown pattern: all cooldowns in `$COOLDOWN_DIR` = `logs/healthz-cooldowns/`; bash: check `stat -c %Y` + `COOLDOWN_HOURS=4`; Python: `Path.stat().st_mtime`; keys: `drift_{hash}`, `dashboard_timer_inactive`, `reconcile_{market}`
- 2026-04-30: P1 Telegram noise Round 2 (commit 6d9ec254) — Fix 1: `_handle_held_stops` skips orders with `order_class in ('oco','bracket','oto')` — Alpaca OCO stop legs are permanently HELD by design; OCO migration (35d2286a) introduced infinite cancel+resubmit loop for CAT/MU; both entries removed from `data/stops_held_state.json`; Fix 2: `run_position_monitor()` in eod_settlement.py gets daily dedup: `_EOD_MONITOR_STATE_FILE=data/eod_position_monitor_state.json`; evaluate_all(send_telegram=True) only fires once per calendar day (3 concurrent market cron entries all call it at 08:00 UTC — first wins, rest suppressed); state NOT written when no alerts; Fix 3: `check_portfolio_drawdown` filters equity_history to `date >= _ATTRIBUTION_RESET_DATE='2026-04-29'` (pre-fix entries used global broker equity ~$5189 as denominator, current per-market equity ~$1233 → false 78% DD alert); falls back to starting_equity when no post-reset history; 19/19 tests in tests/test_telegram_noise_regression.py
- 2026-04-29: broker.py syntax error — `brokers/alpaca/broker.py` line 818 has em-dash `—` in comment causing SyntaxError; pre-existing, blocks `test_alpaca_broker.py` + `test_native_bracket_order.py` (do not fix without Worker C ownership)
- 2026-04-29: HALT file test isolation — `tests/conftest.py` gains 3 autouse fixtures: `_isolate_halt_file_session` (session-scope, patches _ks._HALT_FILE before module-level fixtures), `_isolate_halt_file` (function-scope, gives each test fresh tmp HALT path), `_zz_verify_no_halt_pollution` (session-end guard: asserts production path not touched, auto-cleans + pytest.fail if violated); `tests/test_halt_isolation.py` self-tests (2 tests); root cause: test_per_market_drawdown.py tests calling check_daily_drawdown() without patching kill_switch.halt() wrote to production data/HALT; commit dede8d62
- 2026-04-29: Other prod-path pollution risks (audit, NOT fixed): `brokers/pdt_state.py:_STATE_FILE` = data/pdt_state.json (public API has `_path=` param, mitigated); `scripts/sync_protective_orders.py:_HELD_STATE_FILE/_PDT_STATE_FILE` = data/*.json (tests patch _load/_save functions directly — OK); `services/chat_db.py:CHAT_DB_PATH` = data/chat.db (no direct test writes — low risk); `brokers/price_arbiter.py:_THROTTLE_PATH` = data/price_arbiter_alert_throttle.json (no direct test writes — low risk)

- 2026-04-29: Task #281 Phase 1+2 — services/api/ sub-package created; `services/auth.py` (shared check_auth/security, extracted from chat_server to break circular imports); `services/api/__init__.py`; `services/api/finance.py` (GET /api/finance, APIRouter prefix="/api/finance"); `services/api/regime.py` (5 routes: /history /current /forecast /distributions /transitions, APIRouter prefix="/api/regime"); `chat_server.py` mounts both via `app.include_router()`; 382 LOC removed from chat_server (3385→3003); 25/25 new tests in `tests/services/test_finance_router_extraction.py`; commit c50a98ee
- 2026-04-29: router extraction pattern — FastAPI APIRouter stores full path (prefix+sub-path) in route.path; `include_router` does NOT modify original router; circular import avoided by putting shared deps in `services/auth.py` (not cross-importing from chat_server); pre-existing route duplicates in chat_server: /api/monitor (multi-decorator), /api/chat/sessions (3×); /api/regime/forecast + distributions return 500 in test env (missing regime_forecast table + SPY data) — pre-existing data issue, not routing failure
- 2026-04-29: RCA latent #6 — per-market equity attribution + drawdown HWM wiring; commits e58c8557 + b50636c6
  - Fix 1: config/active/{sp500,commodity_etfs,sector_etfs,crypto,defensive_etfs,gold_etfs,treasury_etfs}.json: risk.starting_equity recalibrated from ~5000-5011 (fiction) to real values: sp500=971, commodity_etfs=1001, sector_etfs=3216, inactive markets=0; source: market_equity_history 2026-04-29; last_recalibrated_at added
  - Fix 2: LivePortfolio._get_per_market_equity(current_broker_eq) added — reads market_equity_history, scales alloc by current broker_eq ratio, rejects >3d stale; check_daily_drawdown now uses per-market equity as primary (fallback → global broker_eq → internal equity()); HWM now resets to per-market value, not global; 19 tests in tests/test_per_market_drawdown.py
  - Equity-sum guard: check_equity_config_sum() in scripts/health_check.py + scripts/check_equity_config_sum.py; asserts Σ(active starting_equity) ≤ broker.equity×1.05; Telegram KEEP-LOUD alert on violation; dry_run mode for tests
  - config.risk.starting_equity NOW means per-market allocated equity (not original deposit); update after any significant portfolio rebalance or quarterly recalibration
  - send_message in check_equity_config_sum is imported inside function body — mock at utils.telegram.send_message, not scripts.health_check.send_message
- 2026-04-29: Phase 2 auto-remediation — `core/merge_gates.py` (15 gates, GateResult/GateRunOutcome dataclasses, `run_all_gates()`); `core/merger.py` (MergeOutcome, `push_to_staging()` CAS-update-ref, `_persist_merge_result()` updates fix_attempts+fix_audit_log, `merge_fix()` full pipeline); 78/78 tests in test_merge_gates.py (53) + test_merger.py (25)
- merge_gates.py: gate_no_never_list_touched has **-prefix glob fix: patterns like **/*.sql had empty prefix → old `if prefix and` clause skipped them; fixed to `if not prefix: fnmatch directly`; gate_no_safety_critical_function_modified reads worktree files from disk + git show main: for before-version; will FileNotFoundError for files that don't exist in worktree — caught and reported as violation
- merger.py push_to_staging: uses `git update-ref refs/heads/auto-fix-staging new_sha [cur_sha]` CAS — only advances if staging still points to cur_sha; Atlas has no remote origin (not on GitHub); staging branch advanced locally only; Phase 3 will add auto-promotion from staging → main
- merger.py _persist_merge_result: updates fix_attempts.status (merged/blocked), gates_passed/failed_json, blocked_by_gate; inserts gate_check audit row always + merge audit row only on success; non-fatal (try/except around whole thing); attempt_id=-1 skips persist entirely
- merger.py merge_fix: short-circuits on fix_outcome.success=False (skips all 15 gates); Telegram on failure ONLY (Phase 2 policy); duck-typed fix_outcome (attrs: attempt_id, error_id, fingerprint, branch, worktree, success)

- 2026-04-30: Task #285 Phase 3+7 — chat_server.py decomposition: 3005→1440 LOC (-1565, -52%); 5 new modules: services/api/portfolio.py (10 routes), services/api/health.py (3 routes+helper), services/api/risk.py (5 routes), services/api/research.py (10 routes), services/api/promotions.py (3 routes); all wired via app.include_router() in chat_server.py; 54 new tests + 18 existing all passing; 6 commits pushed
- TestClient pattern (atlas): TestClient(app) WITHOUT auth kwarg (Starlette 0.52.1 doesn't support it); pass auth=("u","p") per-request; monkeypatch "services.auth._get_credentials"; in-memory SQLite must use check_same_thread=False (TestClient uses thread pool); patch db functions at db.atlas_db.X (not services.api.X.func since imports are lazy inside function bodies)
- 2026-04-30: Task #283 Wave 2 bare-except conversion — 5 modules converted (40 unbound except Exception eliminated); 28/28 tests in tests/test_bare_except_wave2.py; commit 428fc3d8
  - utils/claude_circuit_breaker.py: 8→0; all file I/O→OSError; JSON parse→(json.JSONDecodeError, OSError); scan loop→(AttributeError, TypeError)
  - scripts/autoresearch.py: 9→0; queue read→(FileNotFoundError, json.JSONDecodeError, OSError); research.loop imports→(ImportError, AttributeError, ...); flush_digest→(ImportError, OSError, RuntimeError)
  - research/promoter.py: 9→0; datetime.fromisoformat→ValueError; JSON files→(json.JSONDecodeError, OSError); DSR numpy/scipy→(ImportError, AttributeError, ValueError, ZeroDivisionError, TypeError); brain write now logs DEBUG with bound exc var (latent bug surfaced)
  - utils/telegram.py: 6→0; all json.load→(json.JSONDecodeError, OSError); path.read_text→OSError
  - db/atlas_db.py: 8→0; get_db() rollback guard kept as except Exception: # Broad catch intentional (re-raises); parity alert SQLite→sqlite3.Error; ALTER TABLE→sqlite3.OperationalError; JSON parses→(json.JSONDecodeError, KeyError/TypeError)
  - Latent bugs surfaced: promoter.py brain write was silent pass→now DEBUG logged; autoresearch flush_digest was swallowing AttributeError/TypeError; atlas_db ALTER TABLE was masking real DB corruption with broad except
  - Write approach: domain check blocks Edit/Write on atlas/** from Write tool; use `cat > /tmp/fix.py && python3 /tmp/fix.py` pattern for Python-based line replacements
- 2026-05-01: Phase 4 plan rollup (Task #??) — `send_plan_for_approval()` in `services/telegram_bot.py` no longer sends Telegram; now writes `data/plan_notifications_buffer/<market>_<YYYY-MM-DD>.json` atomically (.tmp+os.replace); `send_plan_rollup()` reads all today's buffer files, sends ONE consolidated HTML message; idempotent via `rollup_sent_<date>.txt` sentinel; TTL cleanup 7d on successful send; `notify-rollup` case in pi-cron.sh; `45 19 * * 1-5` cron entry in atlas.crontab; buffer dir created on first write; HALTED buffer written when plan file missing + market halted; 19 tests in `tests/test_plan_rollup.py`; commits b303ebab + c6fedaf3; RISK: auto_approve=True unconditionally re-approves REJECTED plans (pre-existing issue in approve_plan()); manually-rejected plans should use auto_approve=False or the cron should not call send_plan_for_approval on them; patch target for tests is `services.telegram_bot.get_active_config` NOT `utils.config.get_active_config` (from-import binding)
- 2026-05-01: Bug class CROSS-MARKET HWM INCOMPATIBILITY (HALT fixed) — Root cause: (1) FCX in live_sp500.json but derive_universe('FCX') returns 'commodity_etfs'; (2) market_equity_history snapshot attributed FCX MV to commodity_etfs (via derive_universe); (3) Task #282 (2026-04-29) removed FCX from markets/etf_markets.py CommodityETFsMarket → _refresh_from_broker() filtered it out → self.positions for commodity_etfs lacked FCX → per_market_eq=$1003 (understated) vs HWM=$1297.55 → phantom dd=22.7% → false HALT. Fix: restore FCX to CommodityETFsMarket.get_universe_tickers(); update KNOWN_OVERLAPS in disjointness tests to allow commodity_etfs∩sp500={FCX}; hwm_date=NULL (JSON+SQLite) to force session reset; HALT cleared. RULE: markets/etf_markets.py must be superset of universe/definitions.py for the same market — any ticker in definitions.py but not in etf_markets.py will be in snapshots but NOT in live formula → phantom drawdown. scripts/validate_state_universes.py exits 1 if cross-market positions detected (run from cron preamble).
