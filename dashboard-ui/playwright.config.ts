import { defineConfig, devices } from '@playwright/test'

/**
 * Playwright config for dashboard-ui lifecycle Controls tests.
 *
 * Tests mock all backend calls via page.route() so no live backend is required.
 * `webServer` boots Vite dev server with VITE_ENABLE_CONTROLS_TAB=true so the
 * Controls tab is rendered.
 */
export default defineConfig({
  testDir: './tests',
  testMatch: '**/*.spec.ts',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: [['list'], ['html', { open: 'never' }]],
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'VITE_ENABLE_CONTROLS_TAB=true npm run dev -- --port 5173',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
})
