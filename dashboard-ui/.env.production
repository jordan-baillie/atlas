# Phase 2 dogfood: Controls tab enabled in production builds.
# Vite reads this automatically when running `npm run build` (mode=production).
# To override locally for an OFF build: VITE_ENABLE_CONTROLS_TAB=false npm run build
VITE_ENABLE_CONTROLS_TAB=true
