# Builder-2 Completion Note

## Changes Made (in /root/pi-swarm)

### extensions/swarm/merge.ts
- Removed break from mergeAll() — now tries ALL branches regardless of conflicts
- Added runPostMergeTests(pi, repoRoot, command) returning { passed: boolean, output: string }
- Cleaned up incorrect additions from a partial prior implementation

### extensions/swarm/worktree.ts
- Added createReviewWorktree(pi, repoRoot, builderBranches, baseBranch) — sequential best-effort merges
- Added removeReviewWorktree(pi, repoRoot, worktreePath) — cleanup by path string

## Commit
9436ea0 on /root/pi-swarm master
