# Atlas trading system - pinned dependencies
# IMPORTANT: Always install with: pip install --break-system-packages -r requirements.txt
# Never run bare `pip install numpy` or `pip install --upgrade scipy` alone.
# numpy and scipy MUST be upgraded together to avoid ABI incompatibility.

# Core scientific stack (keep these in sync!)
numpy==2.4.2
scipy==1.17.1
pandas==3.0.1

# Market data
yfinance==1.2.0

# Brokers
moomoo-api==9.6.5608
alpaca-py==0.43.2
ib-insync==0.9.86

# Utilities
requests==2.32.5
