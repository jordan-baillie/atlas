# CHANGELOG-NOTES.md

Archaeology pointers and context notes for commits that are hard to trace
from `git log` alone.  Add entries chronologically (newest first).

---

## 2026-05-07 — Commit `0c11420e` and `ee978b3a` archaeology pointer

These two commits contain bundled overlay-related fixes that were committed together
during the parallel-worker round on 2026-05-07. If you're tracing the history of
overlay-shadow-mode wiring or same-bar round-trip recording behavior, see:
- `0c11420e` — overlay enabled-flag fail-closed default + same-bar reconcile fix bundled
- `ee978b3a` — followup fixes

Do NOT rebase. Verbatim contents preserved.
