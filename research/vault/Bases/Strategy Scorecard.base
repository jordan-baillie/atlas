filters:
  and:
    - file.hasTag("strategy")
    - file.inFolder("Strategies")
views:
  - type: table
    name: "Strategy Scorecard"
    order:
      - file.name
      - note.status
      - note.total_experiments
      - note.best_sharpe
